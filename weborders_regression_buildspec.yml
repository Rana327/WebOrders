version: 0.2

phases:
  install:
    runtime-versions:
      java: openjdk8
    commands:

  build:
    commands:
      - mvn clean test -Dcucumber.options="--tags '@UITest and @Regression'" -DexecutionPlatform="AWS_CHROME"

  post_build:
    commands:
      - aws s3 cp . s3://837311-abhinash-bootcamp-results/regression/ --recursive --exclude "*" --include "*.html"
